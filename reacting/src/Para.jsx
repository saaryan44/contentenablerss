
const Para=({name})=>{let pp="";if(name==="Concepts"){pp="The key learning areas in this section.";}else if(name==="Scenarios"){pp="Potential situations that you might face.";}else{pp="All instructional information to download and use.";} return(<p>{pp}</p>);};

export default Para;